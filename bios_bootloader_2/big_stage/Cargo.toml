[package]
name = "big_stage"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "big_stage"
test = false
bench = false

[dependencies]
x86_64 = { version = "0.15.5", default-features = false, features = ["instructions"] }

[build-dependencies]
common = { version = "0.1.0", path = "../common" }
