[package]
name = "bios_bootloader"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "bios_bootloader"
test = false
bench = false

[dependencies]

[build-dependencies]
scripts = { path = "../scripts" }
